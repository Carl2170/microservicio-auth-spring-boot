type Query {
    getUser(id: ID!): User,
    getAllUsers: [User],
    getUsers(page: Int, size: Int, filter: String): UsersPage,
    obtenerEmpleados: [Empleado],
        obtenerClientes: [Cliente]
}

